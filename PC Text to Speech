#-----PC Text-To-Speech-----

PC_IP = "192.168.68.50" #Personal PC IP Address 

def speak_on_pc(word):
    try:
        url = f"http://{PC_IP}:5000/say?word={word}"
        print("Sending to PC:", url)
        urequests.get(url)  #fire-and-forget/ send request then continue without waiting for response
    except:
        pass
